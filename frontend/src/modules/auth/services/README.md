# Auth Services Package

Chứa các khai báo endpoints gọi API (RTK Query Slices) liên quan đến nghiệp vụ Xác thực.

## Ví dụ
- `authApi.ts`: Inject các endpoints như `login`, `register`, `logout` vào Redux RTK Query `baseApi`.
- Đặt tên file theo chuẩn `camelCase` (ví dụ: `authApi.ts`).
