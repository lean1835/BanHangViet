# Auth Types Package

Định nghĩa các TypeScript interfaces và Types riêng biệt phục vụ cho dữ liệu của module Xác thực.

## Ví dụ
- `IUserCredentials.ts`, `TAuthResponse.ts`.
- Đặt tên file theo chuẩn `PascalCase` (bắt đầu bằng tiền tố `I` cho Interface hoặc `T` cho Type).
