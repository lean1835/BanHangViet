# Constant & Enum Layer

Thư mục này lưu trữ các hằng số và Enum dùng chung cho toàn bộ dự án.

## Quy tắc thiết kế
- Chỉ chứa các Enum nghiệp vụ (ví dụ: `Role`, `OrderStatus`) hoặc các hằng số tĩnh không thay đổi.
- Tên Enum/Constants viết hoa ngăn cách bởi dấu gạch dưới (`SCREAMING_SNAKE_CASE`).
